


# 克隆项目
git clone https://github.com/wddqing/spug.git
cd spug

# 安装Python 3.12 环境
mkdir spug_api/python
cd spug_api/python
sudo apt install -y build-essential zlib1g-dev libssl-dev libffi-dev libreadline-dev
wget https://www.python.org/ftp/python/3.12.0/Python-3.12.0.tgz  # 下载最新版
tar -xzf Python-3.12.0.tgz && cd Python-3.12.0
./configure --enable-optimizations  # 启用性能优化
make -j$(nproc)                     # 多核编译
sudo make altinstall                # 避免覆盖系统 Python
python3.12 --version

# 创建运行环境
## ubuntu安装依赖
sudo apt install python3-dev gcc libldap2-dev rsync sshfs
sudo apt install libmariadb-dev 或者 sudo apt install libmysqlclient-dev （libmysqlclient-dev 能安装 pip install mysqlclient 成功）

Ubuntu 20.04+ 默认把 MySQL 换成 MariaDB，因此 libmariadb-dev 就能满足；
如果你坚持原版 MySQL，可换 sudo apt install libmysqlclient-dev。

## 创建虚拟环境
cd /data/spug/spug_api
python3.12 -m venv venv
source venv/bin/activate

# 安装python包
pip install -U pip setuptools -i https://pypi.tuna.tsinghua.edu.cn/simple/
pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple/
pip install gunicorn mysqlclient -i https://pypi.tuna.tsinghua.edu.cn/simple/

# 配置数据库
后端默认使用的 Sqlite 数据库，通过修改配置使用 MYSQL 作为后端数据库
在 spug_api/spug/ 目录下创建 overrides.py 文件，启动后端服务后会自动覆盖默认的配置，避免直接修改 settings.py 以便于后期获取新版本。

DEBUG = False

DATABASES = {
    'default': {
        'ATOMIC_REQUESTS': True,
        'ENGINE': 'django.db.backends.mysql',
        'NAME': 'spug',             # 替换为自己的数据库名，请预先创建好编码为utf8mb4的数据库
        'USER': 'spug',        # 数据库用户名
        'PASSWORD': '666',  # 数据库密码
        'HOST': '127.0.0.1',        # 数据库地址
        'OPTIONS': {
            'charset': 'utf8mb4',
            'sql_mode': 'STRICT_TRANS_TABLES',
            #'unix_socket': '/opt/mysql/mysql.sock' # 如果是本机数据库,且不是默认安装的Mysql,需要指定Mysql的socket文件路径
        }
    }
}

CACHES = {
    "default": {
        "BACKEND": "django_redis.cache.RedisCache",
        "LOCATION": "redis://127.0.0.1:6379/1",
        "OPTIONS": {
            "CLIENT_CLASS": "django_redis.client.DefaultClient",
        }
    }
}

CHANNEL_LAYERS = {
    "default": {
        "BACKEND": "channels_redis.core.RedisChannelLayer",
        "CONFIG": {
            "hosts": [("127.0.0.1", 6379)],
            "capacity": 1000,
            "expiry": 120,
        },
    },
}

# 初始化数据库
cd /data/spug/spug_api
python manage.py updatedb

# 创建管理员
python manage.py user add -u admin -p spug.cc -s -n 管理员
# -u 用户名
# -p 密码
# -s 超级管理员
# -n 用户昵称

# 创建启动服务脚本
/etc/supervisord.d/spug.conf
[program:spug-api]
command = bash /data/spug/spug_api/tools/start-api.sh
directory = /data/spug/spug_api
autostart = true
stdout_logfile = /data/spug/spug_api/logs/api.log
redirect_stderr = true

[program:spug-ws]
command = bash /data/spug/spug_api/tools/start-ws.sh
directory = /data/spug/spug_api
autostart = true
stdout_logfile = /data/spug/spug_api/logs/ws.log
redirect_stderr = true

[program:spug-worker]
command = bash /data/spug/spug_api/tools/start-worker.sh
directory = /data/spug/spug_api
autostart = true
stdout_logfile = /data/spug/spug_api/logs/worker.log
redirect_stderr = true

[program:spug-monitor]
command = bash /data/spug/spug_api/tools/start-monitor.sh
directory = /data/spug/spug_api
autostart = true
stdout_logfile = /data/spug/spug_api/logs/monitor.log
redirect_stderr = true

[program:spug-scheduler]
command = bash /data/spug/spug_api/tools/start-scheduler.sh
directory = /data/spug/spug_api
autostart = true
stdout_logfile = /data/spug/spug_api/logs/scheduler.log
redirect_stderr = true


# 创建前端nginx配置文件
server {
        listen 80;
        server_name _;     # 修改为自定义的访问域名
        root /data/spug/spug_web/build/;
        client_max_body_size 20m;   # 该值会影响文件管理器可上传文件的大小限制，请合理调整

        gzip  on;
        gzip_min_length  1k;
        gzip_buffers     4 16k;
        gzip_http_version 1.1;
        gzip_comp_level 7;
        gzip_types       text/plain text/css text/javascript application/javascript application/json;
        gzip_vary on;

        location ^~ /api/ {
                rewrite ^/api(.*) $1 break;
                proxy_pass http://127.0.0.1:9001;
                proxy_read_timeout 180s;
                proxy_redirect off;
                proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        }

        location ^~ /api/ws/ {
                rewrite ^/api(.*) $1 break;
                proxy_pass http://127.0.0.1:9002;
                proxy_http_version 1.1;
                proxy_set_header Upgrade $http_upgrade;
                proxy_set_header Connection "Upgrade";
                proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        }

        location / {
                try_files $uri /index.html;
        }
}

# 下载web资源和页面
cd /data/spug/spug_web
wget https://github.com/openspug/spug/releases/download/v3.3.3/web_v3.3.3.tar.gz
mkdir /data/spug/spug_web/build
tar xf web_v3.3.3.tar.gz -C /data/spug/spug_web/build